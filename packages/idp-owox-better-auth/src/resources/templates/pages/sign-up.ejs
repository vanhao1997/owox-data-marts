<!-- Sign Up Content -->
<div class="space-y-6 min-w-[300px] max-w-md">
  <!-- Page Heading -->
  <h1 class="text-2xl font-semibold text-foreground tracking-tight">
    Tạo tài khoản P2PDigital
  </h1>
  <% const enabledProviders = providers || {}; %>
  <% if (errorMessage) { %>
    <div class="rounded-md border border-red-200 bg-red-50 text-red-700 px-4 py-3 text-sm">
      <%= errorMessage %>
    </div>
  <% } %>
  <% if (infoMessage) { %>
    <div class="rounded-md border border-emerald-200 bg-emerald-50 text-emerald-700 px-4 py-3 text-sm">
      <%= infoMessage %>
    </div>
  <% } %>

  <!-- Social Authentication Buttons -->
  <div class="space-y-3">
    <% if (enabledProviders.google) { %>
      <button
        id="google-signup"
        data-social-auth-button
        type="button"
        class="w-full inline-flex justify-center items-center gap-2 py-2.5 px-4 border border-border rounded-md text-sm font-medium text-foreground bg-secondary hover:bg-muted transition-colors disabled:opacity-60 disabled:cursor-not-allowed"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 48 48">
          <path fill="#FFC107" d="M43.611,20.083H42V20H24v8h11.303c-1.649,4.657-6.08,8-11.303,8c-6.627,0-12-5.373-12-12c0-6.627,5.373-12,12-12c3.059,0,5.842,1.154,7.961,3.039l5.657-5.657C34.046,6.053,29.268,4,24,4C12.955,4,4,12.955,4,24c0,11.045,8.955,20,20,20c11.045,0,20-8.955,20-20C44,22.659,43.862,21.35,43.611,20.083z"></path><path fill="#FF3D00" d="M6.306,14.691l6.571,4.819C14.655,15.108,18.961,12,24,12c3.059,0,5.842,1.154,7.961,3.039l5.657-5.657C34.046,6.053,29.268,4,24,4C16.318,4,9.656,8.337,6.306,14.691z"></path><path fill="#4CAF50" d="M24,44c5.166,0,9.86-1.977,13.409-5.192l-6.19-5.238C29.211,35.091,26.715,36,24,36c-5.202,0-9.619-3.317-11.283-7.946l-6.522,5.025C9.505,39.556,16.227,44,24,44z"></path><path fill="#1976D2" d="M43.611,20.083H42V20H24v8h11.303c-0.792,2.237-2.231,4.166-4.087,5.571c0.001-0.001,0.002-0.001,0.003-0.002l6.19,5.238C36.971,39.205,44,34,44,24C44,22.659,43.862,21.35,43.611,20.083z"></path>
        </svg>
        <span>Tiếp tục với Google</span>
      </button>
    <% } %>
    <% if (enabledProviders.microsoft) { %>
      <button
        id="microsoft-signup"
        data-social-auth-button
        type="button"
        class="w-full inline-flex justify-center items-center gap-2 py-2.5 px-4 border border-border rounded-md text-sm font-medium text-foreground bg-secondary hover:bg-muted transition-colors disabled:opacity-60 disabled:cursor-not-allowed"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 23 23">
          <rect width="10" height="10" x="1" y="1" fill="#f35325"></rect>
          <rect width="10" height="10" x="12" y="1" fill="#81bc06"></rect>
          <rect width="10" height="10" x="1" y="12" fill="#05a6f0"></rect>
          <rect width="10" height="10" x="12" y="12" fill="#ffba08"></rect>
        </svg>
        <span>Tiếp tục với Microsoft</span>
      </button>
    <% } %>
  </div>

  <% if (enabledProviders.email) { %>
    <!-- OR Divider -->
    <div class="relative">
      <div class="absolute inset-0 flex items-center">
        <div class="w-full border-t border-border"></div>
      </div>
      <div class="relative flex justify-center text-sm">
        <span class="px-2 bg-card text-muted-foreground">HOẶC</span>
      </div>
    </div>

    <form id="signup-form" class="space-y-4" novalidate>
      <div class="space-y-1">
        <label for="signup-email" class="block text-sm font-medium text-foreground">Email</label>
        <input
          id="signup-email"
          name="email"
          type="email"
          autocomplete="email"
          required
          class="w-full rounded-md border border-border bg-background px-3 py-2 text-sm text-foreground focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary"
          placeholder="you.data.hero@example.com"
        />
      </div>
      <button
        id="signup-submit"
        type="submit"
        class="w-full inline-flex justify-center items-center gap-2 py-2 px-4 rounded-md text-sm font-medium text-primary-foreground bg-primary hover:bg-primary/90 transition-colors disabled:opacity-60"
      >
        Gửi liên kết đăng ký
      </button>
      <p id="signup-status" class="text-sm min-h-[20px]" aria-live="polite"></p>
    </form>
  <% } %>

  <p class="text-center text-sm text-muted-foreground">
    Đã có tài khoản?
    <a href="/auth/sign-in" class="font-medium text-primary hover:text-primary/80">Đăng nhập</a>
  </p>
</div>

<%- include('../partials/social-auth') %>
<script>
  const urlParams = new URLSearchParams(window.location.search);
  const redirect = urlParams.get('redirect');
  const magicLinkSignupIntent = '<%= magicLinkSignupIntent %>';
  const signupForm = document.getElementById('signup-form');
  const signupStatus = document.getElementById('signup-status');
  const signupSubmit = document.getElementById('signup-submit');

  const TIMER_KEY = 'signup_email_timer_expiry';

  function setSignupStatus(message, type = 'error') {
    if (!signupStatus) return;
    signupStatus.textContent = message || '';
    signupStatus.classList.remove('text-red-600', 'text-emerald-700', 'text-blue-600');
    if (type === 'success') {
      signupStatus.classList.add('text-emerald-700');
    } else if (type === 'info') {
      signupStatus.classList.add('text-blue-600');
    } else {
      signupStatus.classList.add('text-red-600');
    }
  }

  function getTimeRemaining() {
    const expiry = localStorage.getItem(TIMER_KEY);
    if (!expiry) return 0;
    
    const remaining = Math.max(0, Math.floor((Number(expiry) - Date.now()) / 1000));
    if (remaining === 0) {
      localStorage.removeItem(TIMER_KEY);
    }
    return remaining;
  }

  function startTimer(seconds) {
    const expiry = Date.now() + (seconds * 1000);
    localStorage.setItem(TIMER_KEY, expiry.toString());
    updateTimerUI();
  }

  function updateTimerUI() {
    const remaining = getTimeRemaining();
    if (remaining > 0) {
      signupSubmit.disabled = true;
      setSignupStatus(`Vui lòng chờ ${remaining} giây trước khi yêu cầu liên kết mới.`, 'info');
      setTimeout(updateTimerUI, 1000);
    } else {
      signupSubmit.disabled = false;
      setSignupStatus('');
    }
  }

  // Check for existing timer on page load
  if (getTimeRemaining() > 0) {
    updateTimerUI();
  }

  async function handleSignUp(event) {
    event.preventDefault();
    if (!signupForm || !signupSubmit) return;

    const email = signupForm.email?.value?.trim();
    if (!email) {
      setSignupStatus('Nhập địa chỉ email của bạn.');
      return;
    }

    signupSubmit.disabled = true;
    setSignupStatus('');

    try {
    const response = await fetch('/auth/api/magic-link', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email, intent: magicLinkSignupIntent }),
      });

      const contentType = response.headers.get('content-type') || '';
      if (!response.ok) {
        let message = 'Không thể gửi liên kết. Hãy thử lại.';
        if (contentType.includes('application/json')) {
          const data = await response.json().catch(() => null);
          if (data?.error) message = data.error;
          // Handle rate limiting with waitSeconds from backend
          if (response.status === 429 && data?.waitSeconds) {
            startTimer(data.waitSeconds);
            return;
          }
        } else {
          const text = await response.text().catch(() => '');
          if (text) message = text;
        }
        setSignupStatus(message);
        signupSubmit.disabled = false;
        return;
      }

      // Success - start 60 second timer
      startTimer(60);
      setSignupStatus('Liên kết đăng ký đã được gửi.', 'success');
    } catch (error) {
      console.error('Sign-up access link failed', error);
      setSignupStatus('Không thể gửi liên kết đăng ký. Hãy thử lại.');
      signupSubmit.disabled = false;
    }
  }

  const googleButton = document.getElementById('google-signup');
  const microsoftButton = document.getElementById('microsoft-signup');
  googleButton?.addEventListener('click', () => submitSocial('google'));
  microsoftButton?.addEventListener('click', () => submitSocial('microsoft'));
  signupForm?.addEventListener('submit', handleSignUp);
</script>
